name: Phase 17 Railway E2E

on:
  push:
    branches:
      - develop
      - 'feature/**'
      - 'fix/**'
      - 'perf/**'
    paths:
      - 'src/MachiVerseWorks.Simulation/**'
      - 'src/MachiVerseWorks.Persistence/**'
      - 'src/MachiVerseWorks.Protocol/**'
      - 'src/MachiVerseWorks.Server/**'
      - 'src/web/**'
      - 'tests/fixtures/phase17-railway.save.json'
      - 'scripts/run-phase17-e2e.sh'
      - '.github/workflows/phase17-e2e.yml'
  pull_request:
    branches:
      - develop
    paths:
      - 'src/MachiVerseWorks.Simulation/**'
      - 'src/MachiVerseWorks.Persistence/**'
      - 'src/MachiVerseWorks.Protocol/**'
      - 'src/MachiVerseWorks.Server/**'
      - 'src/web/**'
      - 'tests/fixtures/phase17-railway.save.json'
      - 'scripts/run-phase17-e2e.sh'
      - '.github/workflows/phase17-e2e.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase17-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    name: save-server-browser-railway
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version-file: src/web/.node-version
          cache: npm
          cache-dependency-path: src/web/package-lock.json
      - name: Run Phase 17 end-to-end
        run: bash scripts/run-phase17-e2e.sh
      - name: Upload Phase 17 artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: phase17-e2e-artifacts
          path: .artifacts/phase17-e2e
          if-no-files-found: warn
          retention-days: 7
