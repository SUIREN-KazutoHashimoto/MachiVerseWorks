name: Phase 11 Road Network Benchmark

on:
  push:
    branches:
      - develop
      - 'feature/**'
      - 'perf/**'
    paths:
      - 'src/MachiVerseWorks.Simulation/**'
      - 'benchmarks/**'
      - '.github/workflows/phase11-benchmark.yml'
  pull_request:
    branches:
      - develop
    paths:
      - 'src/MachiVerseWorks.Simulation/**'
      - 'benchmarks/**'
      - '.github/workflows/phase11-benchmark.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase11-benchmark-${{ github.ref }}
  cancel-in-progress: true

jobs:
  benchmark:
    name: road-network-10k-100k
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json
      - name: Restore
        run: dotnet restore MachiVerseWorks.slnx
      - name: Run Road Network benchmarks
        env:
          MACHIVERSE_BENCHMARK_ARTIFACTS: ${{ github.workspace }}/.artifacts/phase11-benchmark
        run: dotnet run --project benchmarks/MachiVerseWorks.Benchmarks/MachiVerseWorks.Benchmarks.csproj --configuration Release --no-restore -- --filter '*RoadNetworkBenchmarks*' --job short
      - name: Upload benchmark artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: phase11-road-network-benchmark
          path: .artifacts/phase11-benchmark
          if-no-files-found: warn
          retention-days: 14
