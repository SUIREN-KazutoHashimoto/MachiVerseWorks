name: Phase 12 Routing Benchmark

on:
  push:
    branches:
      - develop
      - 'feature/**'
      - 'perf/**'
    paths:
      - 'src/MachiVerseWorks.Simulation/**'
      - 'benchmarks/**'
      - '.github/workflows/phase12-routing-benchmark.yml'
  pull_request:
    branches:
      - develop
    paths:
      - 'src/MachiVerseWorks.Simulation/**'
      - 'benchmarks/**'
      - '.github/workflows/phase12-routing-benchmark.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: phase12-routing-benchmark-${{ github.ref }}
  cancel-in-progress: true

jobs:
  benchmark:
    name: routing-small-medium-large
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          global-json-file: global.json
      - name: Restore
        run: dotnet restore MachiVerseWorks.slnx
      - name: Run Routing benchmarks
        env:
          MACHIVERSE_BENCHMARK_ARTIFACTS: ${{ github.workspace }}/.artifacts/phase12-routing-benchmark
        run: dotnet run --project benchmarks/MachiVerseWorks.Benchmarks/MachiVerseWorks.Benchmarks.csproj --configuration Release --no-restore -- --filter '*RoutingBenchmarks*' --job short
      - name: Upload benchmark artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: phase12-routing-benchmark
          path: .artifacts/phase12-routing-benchmark
          if-no-files-found: warn
          retention-days: 14
